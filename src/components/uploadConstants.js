// Constantes do componente UploadZone — usadas por ele e por componentes que
// precisem exibir/validar limites de arquivos. Genéricas o suficiente para
// qualquer formulário do sistema (não pertencem a uma feature específica).

// Tamanho máximo por arquivo (5 MB)
export const MAX_FILE_SIZE = 5 * 1024 * 1024

// Tipos aceitos no input file
export const ACCEPTED_TYPES = '.pdf,.jpg,.jpeg,.png,.doc,.docx'
